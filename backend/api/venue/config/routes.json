{
  "routes": [
    {
      "method": "GET",
      "path": "/venues",
      "handler": "Venue.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/venues/count",
      "handler": "Venue.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/venues/:id",
      "handler": "Venue.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/venues",
      "handler": "Venue.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/venues/:id",
      "handler": "Venue.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/venues/:id",
      "handler": "Venue.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
